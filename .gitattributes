# Normalize line endings to LF for all text files.
# This prevents Windows/Mac CRLF conflicts on tracked source files.
* text=auto eol=lf

# Explicitly mark binary files so git never touches their line endings.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.zip binary
*.tar binary
*.gz binary
